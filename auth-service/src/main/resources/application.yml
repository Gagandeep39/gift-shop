server:
  port: ${server_port:9100}
spring:
  profiles:
    active: dev
  application:
    name: auth-service

# JTW Config
jwt:
  secret: ${jwt_secret:thisisathirtycharacterlongstring}
  expiration: 3600000
